const React = require('react');

const Header = require('./header/header');

class App extends React.Component {
  render() {
    return (
      <div>
        <Header />
      </div>
    );
  }
}

export default App;